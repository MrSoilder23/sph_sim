#pragma once

namespace quartz {

enum class Properties {
    width,
    height,

    position,
    layout,

    background_color,
    color,

    margin_left,
    margin_right,
    margin_top,
    margin_bottom,

    padding_left,
    padding_right,
    padding_top,
    padding_bottom,

    border,
    border_size,
    border_type,
    border_color,

    text_overflow,
    
    font_size,
    font_family,
    text_color,
    text_align
};

}