#pragma once

enum class Properties {
    width,
    height,

    position,

    background_color,
    color,

    margin,
    margin_left,
    margin_right,
    margin_top,
    margin_bottom,

    padding,
    padding_left,
    padding_right,
    padding_top,
    padding_bottom,

    border,
    border_size,
    border_type,
    border_color,

    text_overflow
};